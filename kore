#!/usr/bin/env python3
import sys
import os

script = os.path.join(os.path.dirname(os.path.abspath(__file__)), "kore.py")
if os.path.exists(script):
    os.execvp("python3", ["python3", script] + sys.argv[1:])
else:
    print("Error: kore.py not found in the same directory.")
    sys.exit(1)
